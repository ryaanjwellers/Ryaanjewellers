.btn {
    padding: 15px 30px;
    border: none;
    border-radius: var(--border-radius);
    font-weight: 600;
    cursor: pointer;
    transition: var(--transition);
    display: inline-flex;
    align-items: center;
    gap: 10px;
    font-size: 1rem;
}

.btn-gold {
    background: linear-gradient(45deg, var(--gold), var(--gold-dark));
    color: var(--black);
}

.btn-outline {
    background: transparent;
    border: 2px solid var(--gold);
    color: var(--gold);
}
.splash-logo-container, .header-logo { background: white !important; }
.btn:hover {
    transform: translateY(-3px);
    box-shadow: var(--shadow-gold);
}
